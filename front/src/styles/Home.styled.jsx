import { styled } from 'styled-components';

export const HomeContent = styled.div`
  position: relative;
  z-index: 2;
`;

export const Home = styled.div`
  position: relative;
  overflow: hidden;
`;
